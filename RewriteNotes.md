# RuVox 2.0 — План переписывания

Документ фиксирует архитектурные решения для следующей итерации RuVox.

## Мотивация

Текущая реализация (PyQt6 + Python TTS-pipeline) накопила набор болевых точек:

- Долгий старт (torch + PyQt6 + QWebEngineView грузятся eagerly).
- Переключение формата отображения (Markdown ↔ Plain) занимает 1–2 с даже на небольшом тексте — из-за пересборки `QTextDocument`, difflib-mapping'а и повторного rendering'а.
- Эпизодические подлаги во время воспроизведения (GIL + O(N) поиск по timestamps + синхронная работа с QTextDocument на каждом тике плеера).
- `QWebEngineView` (весь Chromium) подтягивается только ради mermaid-диаграмм.
- Препроцессинг текста по времени сопоставим с самим TTS: 25+ последовательных regex-проходов, ручной `CharMapping`.

Отдельно — желание реализовать фичи из roadmap'а, для которых текущий стек плохо подходит:
- FF 1.1 Preview-диалог нормализации.
- FF 1.2 Редактирование текста перед TTS.
- Поддержка HTML-формата с сохранением оформления.

## Стек

| Компонент | Выбор |
|-----------|-------|
| Shell / UI | Tauri 2 |
| Frontend | React + TypeScript + Mantine (версия 8+) |
| Стилизация | Дефолтная тема Mantine, CSS-переменные, без кастомного CSS на старте |
| TTS Pipeline | Rust (`regex` + `aho-corasick`) |
| Storage | JSON (`serde_json`), схема-совместимая с текущей Python-версией |
| Аудио-плеер | `tauri-plugin-mpv` (обёртка над libmpv, scaletempo2) |
| TTS-движок | Silero через Python-subprocess |
| Дистрибуция | Nix flake |

## Архитектура

```
┌───────────────────────────────────────────┐
│ Tauri process                             │
│ ┌───────────────┐      ┌────────────────┐ │
│ │ React + Mantine│ <──>│ Rust backend    │ │
│ │ (webview)     │ IPC  │ - pipeline      │ │
│ │               │      │ - storage       │ │
│ │               │      │ - libmpv-rs     │ │
│ │               │      │ - tray          │ │
│ └───────────────┘      └────────┬────────┘ │
│                                 │          │
└─────────────────────────────────┼──────────┘
                                  │ stdin/stdout JSON
                                  ▼
                         ┌────────────────────┐
                         │ Python subprocess  │
                         │ (sidecar)          │
                         │ - torch + Silero   │
                         │ - тонкий прокси    │
                         └────────────────────┘
```

## Решения и обоснования

### 1. Tauri + React + Mantine (не Solid/Svelte, не PyQt)

**Почему Tauri.** Webview нативный → старт <500 мс, HTML/Markdown/Mermaid/подсветка кода — в DOM нативно, preview/edit (FF 1.1, 1.2) реализуются как стандартные веб-компоненты. Размер бинарника 10–30 МБ.

**Почему React.** Активное использование LLM для UI-кода. У React экосистема и обучающая выборка на порядок больше, чем у Solid/Svelte — Claude/Copilot генерируют корректный код без доп. инструкций.

**Почему Mantine.** Готовая component-библиотека закрывает ~80% нужных виджетов (Button, Modal, Drawer, Notifications, Form, Table, SegmentedControl и т.д.) плюс `@mantine/hooks` (80+ утилит). Pet-проект — ценность «взял готовое» выше, чем «вылизал каждый пиксель» (что даёт Tailwind+shadcn).

**Caveat по Mantine и LLM.** Был breaking change 6 → 7 (переход с emotion на CSS Modules + CSS-переменные). LLM-ы смешивают v6 и v7 синтаксис. **В CLAUDE.md явно указать:** «используем Mantine 8, стилизация через CSS Modules и prop `classNames`, никакого `sx`/`createStyles`, форма через `@mantine/form` (не react-hook-form)».

### 2. Дефолтная тема Mantine + переключатель

Никакого кастомного CSS на старте. `MantineProvider` с `defaultColorScheme="auto"` (следует системной `prefers-color-scheme`). `primaryColor` — дефолтный.

**Переключатель темы включён в базовый набор.** `SegmentedControl` с тремя опциями: `Light` / `Dark` / `Auto`. Размещение — маленькая иконка/контрол в header'е главного окна (не в настройках — быстрее доступ).

Персистенция — через стандартный `localStorageColorSchemeManager` Mantine. В будущем (polish) можно прокинуть кастомный `colorSchemeManager` с записью в единый `config.json` через Tauri IPC.

Старые PyQt-темы (`Steel Terminal` и др. из `themes.py`) — выбросить при миграции.

### 3. Pipeline на Rust

Одна из главных выгод переписывания: препроцессинг сейчас занимает столько же, сколько сам TTS.

- `regex` крейт (DFA) — 10–100× быстрее Python `re`.
- `aho-corasick` для словарных lookup'ов (IT_TERMS, AS_WORD, GREEK_LETTERS).
- Один проход лексера: текст → массив типизированных спанов (`Word`, `Url`, `Code`, `Number`, …) → диспетчер нормализаторов на спан. Вместо 25 последовательных regex-проходов.
- `CharMapping` строится встроенно, без dual-pass.

**Golden-tests:** сохранить Python-реализацию как reference, для каждого `(input, expected_output)` Rust должен выдавать то же. Снапшот-тесты — и регрессия, и живая документация.

### 4. Python только для Silero, через subprocess

**Не PyO3** (не эмбеддим CPython в Tauri-процесс). Причины:
- Исторический SIGSEGV в torch BERT при неаккуратной инициализации → любой краш Python утащит UI.
- GIL прямо в Tauri-процессе — лишний источник багов.
- Сборка Tauri + embedded Python на практике — боль.

**Не systemd-daemon.** Subprocess под lifecycle Tauri: спавнится при старте, убивается при выходе. Проще и настройки, и дебага. Вопрос долгоживущего daemon'а (с персистентной моделью между сессиями) — отложен до тех пор, пока ждать прогрев при старте реально начнёт раздражать.

**Протокол — минимальный.** Не стримим аудио чанками, не заморачиваемся с framing бинарных данных:

```
stdin  (одна строка JSON):
  {"text": "...", "speaker": "v5_ru", "sample_rate": 48000, "out_wav": "/tmp/ruvox/xyz.wav"}

stdout (одна строка JSON):
  {"ok": true, "timestamps": [...], "duration_sec": 12.3}
  {"ok": false, "error": "..."}
```

- Rust генерирует уникальный `out_wav` path, после успеха забирает WAV с диска.
- Один in-flight request за раз (Silero не thread-safe) → очередь живёт в Rust, корреляция-id не нужна.
- Дебаг прост: WAV можно открыть руками.
- Для новых параметров — опциональные поля в JSON.
- Логи Python на stderr → Rust проксирует в `tracing`.

**Прогрев модели.** Tauri спавнит subprocess в `on_setup`, шлёт первым сообщением `warmup`-команду. К моменту, когда пользователь копирует первый текст, модель загружена.

### 5. Storage — JSON as-is

Схема текущего `history.json` и сопутствующих файлов сохраняется:
- `~/.cache/ruvox/history.json` — версионированный список `TextEntry`.
- `~/.cache/ruvox/audio/{uuid}.wav` — аудио.
- `~/.cache/ruvox/audio/{uuid}.timestamps.json` — тайминги слов.

**Бонус:** схема-совместимость → при переходе не нужна миграция кэша, Rust-версия читает существующий `history.json`. Старое PyQt-приложение и новое Tauri-приложение могут работать с одним кэшем (не одновременно).

На Rust-стороне — `serde_json` + `Vec<TextEntry>` в памяти. SQLite — когда реально понадобится (тысячи записей), не раньше.

### 6. libmpv через `tauri-plugin-mpv`

Сохраняем текущий плеер как есть — там есть качественное масштабирование скорости (scaletempo2), критичное для прослушивания на > 1.0x. `tauri-plugin-mpv` — плагин для Tauri над той же libmpv, что используется сейчас через `python-mpv`. Выбран пользователем как готовая интеграция для Tauri (вместо прямых биндингов `libmpv-rs`). Минимум изменений поведения.

### 7. Хоткеи — не первоочередная фича

По опыту использования: хоткеи редко критичны — обычно пользователь всё равно смотрит в окно при чтении. Реализация хоткеев (через `tauri-plugin-global-shortcut`) — опциональная фича после MVP, не блокер.

## Отложенные вопросы

### Silero и зависимость от torch

Silero заброшен автором (нет активной разработки). Надежда на community ONNX-конверсию слабая. Pet-проект **живёт с Python-subprocess + torch до тех пор, пока:**
- Silero сам по себе удовлетворяет по качеству русского голоса (пока альтернатив с тем же соотношением качество/скорость/оффлайн нет).
- Размер окружения и время прогрева остаются терпимыми.

**Возможные будущие направления** (не приоритет):
- Самостоятельный экспорт Silero в ONNX (сложно из-за TorchScript + BERT homosolver).
- Миграция на другой движок (Piper, VITS-based fine-tunes, XTTS-v2).
- Периодически (раз в квартал) проверять HuggingFace на свежие русские TTS-модели.

Когда/если torch-зависимость уйдёт — Python-subprocess заменяется на Rust-крейт (`ort` — биндинги к onnxruntime).

### Персистенция выбора темы в едином config'е

Сейчас — `localStorage`. Полноценный `colorSchemeManager`, пишущий в общий `config.json` через Tauri IPC — задача на polish-фазу.

### Кэш нормализации

Не описан выше явно, но закладывается дизайном: при pipeline на Rust все результаты (`normalized_text`, `char_mapping`, `timestamps`) хранятся в `TextEntry`. Повторное прослушивание, preview, edit — из кэша без пересчёта.

## План фаз

Спайк-фаза пропущена по решению пользователя — сразу идём в реализацию, `tauri-plugin-mpv` закрывает риск по аудио, возможные проблемы с Nix flake решаются координатором по ходу.

| Фаза | Что |
|------|-----|
| 1 | **Foundation.** Nix flake (Rust + Node + Python), Tauri init (React + TypeScript + Mantine 8), IPC-контракт, storage-схема, ttsd-скелет. |
| 2 | **Rust pipeline.** Порт TrackedText/CharMapping, все нормалайзеры, golden-tests против legacy Python. |
| 3 | **Core UI.** AppShell + theme switcher + очередь + viewer (MD + Plain) + плеер + подсветка слов. |
| 4 | **Enrichment.** Mermaid в DOM + HTML-режим + настройки + нотификации. |
| 5 | **Advanced features.** Preview-диалог (FF 1.1) + edit-mode (FF 1.2). |
| 6 | **Polish.** Nix flake бандлинг, документация, финальные тесты, удаление `legacy/`. |

Сроки не фиксированы — ориентируемся на dependency-граф и параллельность агентов.

## Риск-чекпоинты

После завершения Фазы 1 нужно подтвердить, что фундамент здоров:
1. Python-subprocess стартует и общается с Rust стабильно под Nix flake.
2. `tauri-plugin-mpv` играет со scaletempo2 (качество на 1.5x–2.0x — как в текущей версии).
3. Время старта Tauri-приложения до готового к вводу UI — <1 с.
4. DX с React + TypeScript + Mantine 8 субъективно комфортен.

Если хотя бы один пункт «нет» — дёшево возвращаемся к альтернативам: G2 (Python-стек + subprocess TTS + кэш + markdown-it-py) или G3 (Rust pipeline через PyO3 при сохранении PyQt UI). `git checkout main` возвращает к PyQt-версии.

## Рабочий процесс: ruvox2 branch + параллельные агенты

### Ветки и worktree

- **Целевая ветка:** `ruvox2` (локальная, не пушится в origin до готовности).
- **Интеграционный worktree:** `/home/evgen/work/github/RuVox/` — текущая рабочая копия. Ветка `ruvox2` развивается здесь. Исходный `main` (PyQt-версия) остаётся в истории и доступен через `git checkout main` в любой момент.
- **Worktree'ы агентов:** управляются Claude Code через параметр `isolation: "worktree"` у Agent-тула — создаются в sandbox-allowed месте автоматически, чистятся если изменений не было.
- **Feature-branches агентов:** именуются `task/<slug>`, базируются на `ruvox2`. После merge **не удаляются** — остаются в истории для аудита.

Первоначально рассматривался отдельный интеграционный worktree в соседней папке (`../RuVox-ruvox2/`), но Claude-sandbox не разрешает запись вне проекта. Итог — интеграция в текущем worktree, переключением веток.

### Структура файлов (на `ruvox2`)

| Путь | Назначение |
|------|-----------|
| `src/` | React + TypeScript фронт. |
| `src-tauri/` | Rust-код: pipeline, storage, libmpv-wrapper, subprocess-менеджер, Tauri-команды. |
| `ttsd/` | Python-subprocess с Silero (тонкий прокси, свой pyproject.toml + uv-окружение). |
| `docs/` | Проектная документация (обновляется). |
| `legacy/` | Замороженная PyQt-реализация — референс для копирования логики. Удаляется одним коммитом после достижения фичи-паритета. |
| `RewriteNotes.md` | Этот файл — архитектурные решения. |
| `RewriteTaskPlan.md` | Детальный план задач + граф зависимостей. |
| `task_history.md` | Живой журнал исполнения: какую задачу какой агент делал, статус ревью, проблемы. |

### Делящиеся/изолированные ресурсы

| Ресурс | Поведение |
|--------|-----------|
| Git history | Общий: один репозиторий, разные ветки. |
| Рабочее дерево ruvox2 vs main | Разделены через `git checkout`. |
| Worktree'ы агентов | Физически изолированы, свой checkout каждого. |
| Сборочные артефакты (`target/`, `node_modules/`, `.venv/`) | Раздельные per-worktree. Добавить в `.gitignore`. |
| Кэш приложения (`~/.cache/ruvox/`) | **Общий**. Схема `history.json` совместима с legacy → переключение между PyQt-RuVox (main) и Tauri-RuVox (ruvox2) обратимо без потери истории. |

### Параллельное исполнение задач агентами

**Модель исполнения:** координатор (основная сессия) декомпозирует работу на задачи с зависимостями, запускает до **7 параллельных** `autopilot-sonnet`-агентов. Когда кто-то из агентов завершает работу — сразу запускается следующий ready-task, не дожидаясь остальных.

**Роли:**

| Роль | Модель | Инстансов | Назначение |
|------|--------|-----------|-----------|
| Координатор | Opus (текущая сессия) | 1 | Декомпозиция, task-list, запуск агентов, ведение `task_history.md`. |
| Исполнитель задачи | `autopilot-sonnet` | до 7 параллельно | Реализует одну задачу в изолированном worktree, коммитит в `task/<slug>`. |
| Ревьюер + мержер | `autopilot` на Opus | **строго 1** | Серийная обработка merge-запросов: ревью diff'а, merge в `ruvox2`, запуск post-merge проверок. |

**Строгая единственность ревьюера** нужна, чтобы параллельные merge-операции не создавали race-conditions и merge-конфликтов между собой.

### Life-cycle задачи

1. Задача создаётся в `RewriteTaskPlan.md` + через `TaskCreate` с зависимостями.
2. Как только все `blockedBy`-зависимости закрыты, координатор запускает `autopilot-sonnet`-агента с isolated worktree.
3. Агент реализует задачу → коммитит в `task/<slug>` → отдаёт управление.
4. Координатор добавляет запись в `task_history.md` (агент завершил, ссылка на ветку, short summary).
5. Координатор отдаёт задачу ревьюеру-Opus (ставит в его очередь). Если ревьюер свободен — запускает; если занят — ждёт.
6. Ревьюер:
   - читает diff;
   - прогоняет post-merge чеки (`cargo build`, `cargo test`, `pnpm typecheck`, `ruff check`, `pytest`);
   - если **ок** → merge в `ruvox2`, пометка задачи completed, запись в task_history;
   - если **небольшие ошибки** (стиль, мелкие баги) → спавнит sonnet-агента для доработки **той же ветки**, после фикса перезапускает ревью;
   - если **критичные ошибки** (неверный подход, broken architecture) → сигналит координатору: откатывается коммит, задача перезапускается с уточнённым промптом.
7. Задачи-наследники разблокируются → координатор запускает следующие агенты.

**Если критичная ошибка обнаружена позже, уже после merge** — создаётся новая задача с фиксом, не откатываем.

### Пост-мерж чеки

Ревьюер после каждого merge в `ruvox2` прогоняет (набор зависит от затронутых модулей):
- `cargo build --manifest-path src-tauri/Cargo.toml`
- `cargo test --manifest-path src-tauri/Cargo.toml` (если тесты существуют)
- `pnpm typecheck` (если фронт затронут)
- `pnpm lint` (если фронт затронут)
- `ruff check ttsd/` + `pytest ttsd/tests` (если python-subprocess затронут)
- `cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings` (опционально, не блокер)

До завершения задач F1 (Nix flake) и F2 (Tauri init) не все чеки применимы — применяются выборочно.

### Коммиты и push

- **Коммиты агентов:** в свои `task/<slug>` ветки. Драфты не согласовываются — у координатора есть blanket-разрешение на время переписывания.
- **Merge-коммиты:** делает ревьюер в `ruvox2`. Так же без драфтов.
- **`git push`:** только в самом конце, по явному запросу пользователя (когда ruvox2 готов к слиянию в main).
- **`git checkout main`** — не запрещён, пользователь может в любой момент вернуться к PyQt-версии.

### Финализация переписывания

Когда `ruvox2` достигнет фичи-паритета:
1. Пользователь явно подтверждает готовность.
2. Удаляется `legacy/` отдельным коммитом.
3. Merge `ruvox2` → `main` (обычный merge-commit, без squash — история переписывания ценна).
4. Следующий шаг — `git push` — только по явному запросу.
5. Feature-branches `task/*` остаются в истории (или архивируются тэгами).

### Откат

Если rewrite не взлетает (неприемлемый DX, блокеры в Tauri/libmpv и т.д.) — `git checkout main` возвращает к рабочей PyQt-версии без потерь. ruvox2 остаётся в истории как замёрзший spike.
